describe('Calculator', () => {

  it('should perform sum of two numbers', () => {
    const number1 = 5;
    const number2 = 6;
    const result = number1 + number2;
    expect(result).toBe(11);
  });

});
